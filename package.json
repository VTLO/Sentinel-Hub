{
  "name": "sentinel-hub-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "test": "pnpm -r run test",
    "lint": "prettier --check .",
    "lint:fix": "prettier --write .",
    "typecheck": "tsc -b --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "prettier": "^3.8.1"
  }
}
